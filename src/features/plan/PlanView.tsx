/** STUB — executor "plan" owns this file (see TASK.md in that worktree). */
export function PlanView() {
  return (
    <div className="view">
      <h1 className="title">Plan</h1>
      <div className="card">
        <p className="micro">Your Push / Pull / Legs templates will be editable here.</p>
      </div>
    </div>
  )
}
